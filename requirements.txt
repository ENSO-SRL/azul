httpx>=0.27.0
boto3>=1.34.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
pydantic>=2.0.0
apscheduler>=3.10.0
python-multipart>=0.0.9
pytest>=8.0.0
pytest-asyncio>=0.23.0
# SQLCipher — cifrado en reposo para SQLite (Req 3, TEMPORAL hasta PostgreSQL)
# Activar con: DATABASE_ENCRYPTION_KEY=<hex_key>
# Eliminar cuando se migre a PostgreSQL (RDS gestiona cifrado en reposo).
sqlcipher3-binary>=0.5.3
